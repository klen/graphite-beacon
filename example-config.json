// Comments is allowed here
{
  "interval": "10minute",
  "logging": "debug",

  "alerts": [
    {
      // An another comment
      "name": "Memory",
      "query": "*.memory.memory-free",
      "rules": [
        {
          "level": "critical",
          "operator": "gt",
          "value": 10000000
        }
      ]
    }
  ]
}
